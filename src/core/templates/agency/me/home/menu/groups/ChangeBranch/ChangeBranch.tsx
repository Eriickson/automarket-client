import { MenuItem } from "@chakra-ui/menu";
import React, { FC } from "react";

export const ChangeBranch: FC = () => {
  return (
    <div>
      <MenuItem py="1.5">Cambiar de sucursal</MenuItem>
    </div>
  );
};
