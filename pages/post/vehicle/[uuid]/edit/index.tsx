import React, { FC } from "react";
import { VehicleEditTemplate } from "@/templates";

const VehicleEditPanel: FC = () => {
  return (
    <div>
      <VehicleEditTemplate />
    </div>
  );
};

export default VehicleEditPanel;
